# Changelog

## 0.1.0 (2026-07-23)

- Initial release: `nestjs-orm-drizzle` skill — Drizzle ORM (Postgres)
  schema, the version-gated Relations API/RQB v2, the `DatabaseModule`/
  `DRIZZLE` provider pattern, query-builder vs. relational queries,
  transactions, connection pooling, `drizzle-kit` migrations, and
  repository-isolation review red flags (N+1 avoidance, column
  selection/indexing/pagination, and expand-contract migrations folded in
  from a community reference, reframed for Drizzle).
