# Changelog

## 0.1.0 (2026-07-23)

- Initial release: `angular-ui-primeng` skill — PrimeNG install & `app.config.ts`
  setup, v18 token/CSS-variable theming migration (Aura/Material/Lara/Nora
  presets), standalone tree-shaken component imports, Tailwind interop
  (`tailwindcss-primeui` + `cssLayer` ordering for Tailwind v3/v4),
  design-token style overrides, `MessageService`/`ConfirmationService`/
  `DialogService` + host components, `p-table` server-side data and
  virtual scrolling, v18 component renames, and PrimeNG-specific review
  red flags. Authored from primeng.dev / `llms.txt` / `@primeng/mcp`
  (no upstream agent-skill exists to vendor). Notes Taiga UI as the
  documented swap alternative.
