# Examples

Concrete examples belong here, kept out of `SKILL.md` so the skill stays small.
Claude reads this file only when the skill points to it.

## Example: greeting

**Prompt**

> Run hello-skill.

**Expected behavior**

> 👋 The `example-plugin` skill is installed and working. This is a template —
> replace `SKILL.md` with your real instructions.

## Example: pattern to copy

Use a before/after or input/output pair for anything non-obvious:

```
Input:  <what the user provides>
Output: <what the skill should produce>
```
